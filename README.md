# Taisei Compiler

To complie make sure you have `cmake` and `llvm-dev`.

```shell
$ git clone https://github.com/taisei-lang/tsic.git
$ mkdir tsic.build
$ cd tsic.build
$ cmake -G Ninja ../tsic
$ ninja
```